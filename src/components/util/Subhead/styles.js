import { StyleSheet } from 'react-native'
import { greenOpaque } from 'src/styles/colors'

const styles = StyleSheet.create({
  container: {
    padding: 12,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: greenOpaque,
  },
  text: {
    fontSize: 16,
    color: '#FFFFFF',
  },
})

export default styles
